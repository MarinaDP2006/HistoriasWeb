import ScrollToTop from '../components/ScrollToTop'
import { stories } from '../data/library'

export default function HomePage() {
  const featuredStories = stories.slice(0, 3)

  return (
    <div className="min-h-screen bg-gradient-to-b from-purple-900 to-black">
      <ScrollToTop />
      
      <div className="max-w-7xl mx-auto px-4 py-12">
        <section className="text-center text-white mb-16">
          <h1 className="text-6xl font-bold mb-4 text-purple-300">Biblioteca Fanfic</h1>
          <p className="text-2xl text-gray-300 mb-4">Historias que tocan el corazón</p>
          <p className="text-lg text-gray-400 max-w-3xl mx-auto">
            Bienvenido a la plataforma oficial de Ninai_06. Aquí encontrarás todas mis 
            historias fanfic, originales y ediciones especiales publicadas en Wattpad.
          </p>
        </section>

        <section className="mb-16">
          <h2 className="text-4xl font-bold text-purple-300 mb-8 text-center">
            Las historias más leídas
          </h2>
          <div className="grid grid-cols-1 md:grid-cols-3 gap-8">
            {featuredStories.map((story) => (
              <a
                key={story.id}
                href={story.wattpadLink}
                target="_blank"
                rel="noopener noreferrer"
                className="group cursor-pointer transform transition-transform hover:scale-105"
              >
                <div className="bg-gray-800 rounded-lg overflow-hidden shadow-lg hover:shadow-purple-500/50 border border-purple-600">
                  <img
                    src={story.imageUrl}
                    alt={story.title}
                    className="w-full h-64 object-cover group-hover:opacity-80 transition"
                  />
                  <div className="p-4">
                    <h3 className="text-lg font-bold text-purple-300 mb-1 truncate">
                      {story.title}
                    </h3>
                    <p className="text-sm text-gray-400 mb-2">por {story.author}</p>
                    <span
                      className={`inline-block text-xs font-semibold px-3 py-1 rounded-full ${
                        story.type === 'fanfic'
                          ? 'bg-blue-600 text-white'
                          : 'bg-green-600 text-white'
                      }`}
                    >
                      {story.type === 'fanfic' ? 'Fanfic' : 'Original'}
                    </span>
                  </div>
                </div>
              </a>
            ))}
          </div>
        </section>

        <section className="text-center">
          <h2 className="text-3xl font-bold text-purple-300 mb-6">
            ¿Quieres explorar todas mis historias?
          </h2>
          <a
            href="/historias"
            className="inline-block bg-purple-600 hover:bg-purple-700 text-white font-bold py-3 px-8 rounded-lg transition text-lg"
          >
            Ver todas las historias
          </a>
        </section>
      </div>
    </div>
  )
}
